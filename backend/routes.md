





















2, AUTHETICATION

POST: auth/token                -- người dùng đăng nhập thành công và trả về token
POST: auth/introspect           -- Kiểm tra token của người dùng gửi về có phải của mình không